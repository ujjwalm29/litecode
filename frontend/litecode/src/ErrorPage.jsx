import { useRouteError } from "react-router-dom";

export default function ErrorPage() {
  //   const errorMessage = useRouteError();
  //   console.log(errorMessage);

  return <h2>Unexpected Error. Blame the Developer</h2>;
}
